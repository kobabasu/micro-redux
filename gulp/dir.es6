/**
 * node_modulesが存在するディレクトリからの
 * ドキュメントルートを指定
 */

const root = './admin/';

export const dir = {
  root: root,
  src:  root + 'src/',
  js:   root + 'js/',
  dist: root + 'dist/',
  docs: root + 'docs/src/'
}
